# Next Runtime Direction 2026-04-29

Status:

```text
current near-term direction
post-MANIFEST post-OBSERVE post-grave-viewer
```

## Current state

The project now has:

- shared core truth
- CLI machine bench
- LOVE manifestation shell
- working grave viewer
- live operators:
  - `CONNECT`
  - `CYCLE`
  - `CHOOSE`
  - `OBSERVE`
  - `MANIFEST`

`LOGIC` core branch has also been proven
through CLI scenarios before LOVE hookup.

## What should happen next

The next work should stay in this order:

1. decide operator optionality law
2. hook `LOGIC` into LOVE using the already built grave viewer
3. only then continue into the next operator family

## Why this order

Because `LOGIC` is now the operator
that most strongly tests whether:

- the machine truth is shared
- grave UI is real
- multi-step operator targeting is sustainable

So it should be finished honestly
before moving on to the next content branch.

## Current no-rush items

These are explicitly lower priority right now:

- perfect grave viewer polish
- minor animation cleanup
- visual tuning of every effect state

Those can wait
while machine truth is still expanding.

## Current priority stack

```text
law clarity
then core truth
then CLI verification
then LOVE manifestation
then polish
```

## Short formula

```text
the machine is already real enough
to keep moving forward.
next: decide optional operator use,
then finish LOGIC in LOVE,
then continue.
```
