[⊞ ◈] [⚠ seed 75]

# Anomaly 75

## 0. Status

Observed long-run runtime anomaly in headless survival bot.

This is:

- not a fix document
- not a balance claim
- not a law rewrite

This is a crystall note that records one concrete broken trajectory.

Date:

```text
2026-05-22
```

Seed:

```text
75
```

Runner:

```text
survival bot
```

---

## 1. What Happened

During a long headless survival run,
the game reached a state where:

```text
deck = 6
grave = 91
trump_flow = 8
board_closed = false
```

Broken snapshot tail:

```text
manifest: 1:MINOR-55 | 2:TRUMP-17 | 3:TRUMP-19 | 4:MINOR-73 | 5:TRUMP-11 | 6:TRUMP-20
latent:   1:MINOR-80 | 2:- | 3:- | 4:- | 5:- | 6:-
targets:  1:TRUMP-14 | 2:MINOR-54 | 3:TRUMP-6
runtime:  1:MINOR-82
flow:     TRUMP-8, TRUMP-4, TRUMP-22, TRUMP-15, TRUMP-9, TRUMP-13, TRUMP-16, TRUMP-10
```

The immediate invariant failure was:

```text
board_closed = false
```

More specifically:

```text
latent row collapsed to 1 filled slot and 5 empty slots
```

---

## 2. Immediate Trigger

The visible break happened at step:

```text
212
```

The final action before the break was:

```text
CONNECT
```

That `CONNECT` draw sequence exposed:

1. `ORACLE`
2. inside `ORACLE`, chosen trump = `RECAST`

The event contour was:

```text
CONNECT
-> draw reveals ORACLE
-> ORACLE inspects top 6
-> ORACLE chooses RECAST
-> RECAST resolves
-> board closure fails
```

This was the first moment where the invariant visibly broke.

---

## 3. Important Correction

The visible break at `ORACLE -> RECAST` is **not yet treated as the full root cause**.

Reason:

```text
deck had already fallen to 7 before the final trigger
grave had already grown to 90+
```

That state is already suspicious in its own right.

So the current reading is:

- `ORACLE -> RECAST` is the place where the board visibly failed
- but earlier deck / grave ecology may already have been unhealthy

This note does **not** claim:

```text
ORACLE alone is the root bug
```

It only records:

```text
this is where the run finally broke
```

---

## 4. Why The State Is Suspicious

Two values stand out:

```text
grave = 91
deck = 6
```

This raises an obvious ecology question:

```text
where was SHUFFLE?
```

Current concern:

- too much material had already left `deck`
- too much residue had accumulated in `grave`
- not enough circulation had returned material into `deck`

Possible contributing factors:

1. incomplete runtime coverage of all 22 trumps
2. current survival policy pathing
3. insufficient reshuffle / circulation pressure in the observed chain
4. specific chain interactions not yet preserving long-run ecology

This note does **not** choose one cause yet.

---

## 5. Recorded Chain Context

Immediately before the failure,
the run had already passed through many strong events, including:

- `RUSH`
- `FOOL`
- `RESET`
- `HALT`
- `RECAST`
- `ORACLE`
- repeated `OBSERVE` activation of hidden trumps

So this is not an early-run failure.

It is a **late long-run anomaly**
that emerges only after heavy ecology churn.

---

## 6. Current Interpretation

`Anomaly 75` should currently be read as:

```text
long-run circulation anomaly
with visible board-closure failure at ORACLE -> RECAST
```

Not:

```text
simple single-card bug
```

And not:

```text
confirmed balance conclusion
```

---

## 7. What We Are Not Doing Now

This note is intentionally conservative.

We are **not** doing any of the following just because of this run:

- rewriting `ORACLE` law
- rewriting `RECAST` law
- rewriting `SHUFFLE` law
- changing minor draw economy
- changing deck size assumptions

This anomaly is recorded first,
to be revisited later if similar long-run failures repeat.

---

## 8. Short Formula

```text
seed 75 reached a late-run ecology collapse.
Visible break occurred at ORACLE -> RECAST.
Root cause remains open.
```

