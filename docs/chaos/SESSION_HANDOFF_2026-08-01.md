# Хендофф: состояние работы на 2026-08-01

Статус:

```text
слой: chaos
вид: передача состояния холодной сессии
читать: первым, до кода
предыдущий: SESSION_HANDOFF_2026-07-27.md
```

Входная дверь для машины, открывающей репозиторий без контекста разговора. Всё ниже проверяемо в репозитории. Ничего не хранится только в памяти.

---

## 1. Где мы

```text
ветка            honest-observation
последний коммит 302f2d0 Let known cards move through the flow ring
рабочее дерево   56 файлов изменено и не закоммичено
проверки         54 OK, 1 SKIP, 0 FAIL, 0 registry errors
```

**Ничего не закоммичено с 302f2d0.** Вся работа 29 июля — 1 августа в рабочем дереве.

Первое, что стоит сделать: прогнать `lua cli.lua check` и убедиться, что 54/54.

## 2. Метод

Загружен скилл `process` (`~/.claude/skills/process/SKILL.md`). Четыре слоя:

```text
⋯ chaos      давление, мнение допустимо, НЕ контракт
⊞ table      канонический закон
◈ crystall   контракт реализации
▲ manifest   код
```

Правила, которые нарушаются чаще всего:

```text
читать ЗАКОН, а не код       код младший слой. Половина предмета
                             существует только на бумаге
утверждение из памяти        гипотеза, пока не сверено с документом.
                             Касается и автора корпуса
чинить СТАРШИЙ ложный слой   не симптом
тест цитирует ◈ crystall     НИКОГДА ⊞ table
заменённое ПОМЕЧАЕТСЯ        не удаляется
не производить артефакт      пока решения не существует
```

## 3. Что решено за эти дни

**Поражение** реализовано и работает в живой партии:

```text
шаг 9  WIN CHECK    победа
шаг 10 LOSE CHECK   поражение: нет ни одной легальной пары
```

Замер: 12 партий из 12 кончаются `defeat/no_legal_move`, зависаний нет.

**Дека получила законы:**

```text
DECK_LAW           топдек — ЗОНА; тело деки закрыто; отправка по классу
DECK_MODULE_LAW    владение, три операции, шаги, три просителя
```

**Написаны законы модулей новой архитектуры, шесть:**

```text
TRANSITION_LAW       слой транзакции. ОПИСЬ работающего кода
DECK_LAW             правила деки
DECK_MODULE_LAW      владение декой
MINOR_MACHINE_LAW    секвенсор хода, владение, §10 требования
TRUMP_MACHINE_LAW    диспетчер событий, дескриптор козыря
TRANSIENT_ZONE_LAW   play, топдек, прото-рука
```

**Все зоны обрели владельцев:**

```text
дека, латент   модуль деки
манифест       минорная машина
runtime        минорная машина        RUNTIME_ZONE_LAW §10.1
цели           правила в TARGET_ZONE_LAW, гарантия у деки §6.1
очередь        козырная машина
исход          модуль победы
закрытость     НИЧЬЯ: конъюнкция трёх локальных гарантий
```

## 4. Принятое решение о переписывании

```text
архитектура новая. Старый код вытесняется, не чинится
```

`ARCHITECTURE_MIGRATION_2026-07-31`. Порядок жёсткий:

```text
1. модуль deck            ЛИСТ: его просят все, он не просит никого
2. механизм выбора        владелец — минорная машина, козырная потребляет
3. козырная машина        на готовом механизме и готовом deck
4. 2D-рендер              ПОСЛЕ обеих машин, FIRST_REWORK_2D_RENDER
```

Форма: модуль пишется рядом, приёмка проверками, потом старый файл удаляется. **На каждом шаге игра работает.**

54 проверки переживают переписывание: они цитируют кристаллы, а не реализацию. Связность слабая — два структурных грепа по `src/core/win.lua` и ~13 внутренних require.

## 5. Следующий шаг

```text
кристалл модуля deck
```

Таблицы для него готовы: `DECK_LAW` + `DECK_MODULE_LAW`. Открытых блокеров нет.

## 6. Ловушки. Это самое ценное в документе

**Читать закон, а не код.** За два дня шесть моих выводов оказались неверны, и все шесть — от чтения кода с выводом о замысле: про выходы из целевой зоны, про EJECT, про плотность козырей, про дефицит MANIFEST, про десятый шаг, про §7a. Документы старше кода и полнее его.

**Автор — оракул.** Четыре расхождения закона и кода из шести нашлись так: он произнёс правило по памяти, машина сверила. Его утверждения проверяемы и чаще верны.

**Эталоны трасс РАЗОШЛИСЬ намеренно.** 12 из 20, причина одна: переименование `step_check_*` в `step_win_check_*` / `step_lose_check_*`. Перезапись предписана `STEP_CHECK_SLICE §7` последней, после зелёных проверок. **Не «чинить» их и не перезаписывать без команды.**

**`bench survival` сид 6 падает.** `setup_board_closed`, шаг 244, дека пуста. Проверено `git stash` — падало и до правок. Причина: `SHUFFLE` заперт в целевой зоне, единственный путь возврата могилы в деку. Это второе условие поражения, `EMPTY_DECK_2026-07-31`, ещё не реализовано. **Не гоняться.**

**Имя модуля исхода.** В четырёх законах написано «модуль исхода», а закон называется `WIN_MODULE_LAW`, и `§9.3` держит имя открытым. Расхождение **известно и сознательно отложено** решением автора 2026-08-01. Не переоткрывать.

**Класс ошибки «PLACE раньше TAKE».** `reveal_minor_to_grave` не убирает карту из зоны — вызывающий обязан сделать это сам. Нарушение даёт карту в двух зонах, `pop_topdeck` не может осушить деку, и `open_manifest_closure` виснет навсегда. Записано `DECK_MODULE_LAW §5.4`.

**`pairs()` в Lua без порядка.** Тесты, обходящие `state.cards`, обязаны идти по `sorted_ids`. Один тест уже был зелёным по случайности.

**Проверка утечки сравнивает ЦЕЛЫМ ТОКЕНОМ.** `MINOR-3` — префикс `MINOR-34`. Обе реализации искали подстроку и обе врали. Исправлено, `MACHINE_CLI_SLICE §12.6`.

**zsh не разбивает `$var` на слова.** Кусало трижды.

## 7. Что открыто и НЕ блокирует

```text
runaway липкий флаг, должен стать терминальным   TURN_STEP_LAW §13.2
дека пуста, просит козырь                        DECK_MODULE_LAW §8
второе условие поражения (пустая дека)           EMPTY_DECK_2026-07-31
форма механизма выбора                           MINOR_MACHINE_LAW §11
ревизия 22 черновиков козырей                    TRUMP_MACHINE_LAW §14.4
корпус не проверяет сам себя                     решено НЕ чинить
```

## 8. Что НЕ делать

```text
не коммитить без команды
не перезаписывать эталоны без команды
не чинить survival сид 6
не патчить turn.lua: он переписывается, требования в §10
не переоткрывать имя модуля исхода
не писать документ, пока решения не существует
```

---

Передача: Opus 5 (claude-opus-5), 2026-08-01.
Проверено в репозитории, не по памяти.

---

machines only. not for humans.
